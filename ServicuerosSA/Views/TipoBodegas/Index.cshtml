@model IEnumerable<ServicuerosSA.Models.TipoBodega>

@{
    ViewData["Title"] = "Index";
}

<h2>Bodegas por clasificacion</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Crear una nueva bodega</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Detalle)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                <a class="btn btn-info" asp-action="Edit" asp-route-id="@item.TipoBodegaId">Editar</a> |
                <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.TipoBodegaId">Detalles</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.TipoBodegaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
